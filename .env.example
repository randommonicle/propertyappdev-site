# This site uses Netlify Forms for contact form submissions — no environment
# variables are required for the core site.
#
# If you later add Netlify Functions (e.g. for a server-side contact handler
# or API proxy), add any required variable names here without values:
#
# RESEND_API_KEY=
# CONTACT_EMAIL=
